import { Redirect } from 'expo-router';

/**
 * Точка входа.
 *
 * Приложение открывается сразу на списке заметок: онбординга, входа и импорта
 * (плита 01 вайрфрейма) здесь нет — всё хранится локально, аккаунт не нужен,
 * и показывать до содержимого нечего.
 */
export default function Index() {
  return <Redirect href="/notes" />;
}
